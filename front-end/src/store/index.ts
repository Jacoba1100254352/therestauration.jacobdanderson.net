import { createStore } from "vuex";
import axios from "axios";

const state = {};

const mutations = {};

const actions = {};

const getters = {};

export default createStore({
	state,
	mutations,
	actions,
	getters,
});
